# Everything here is development scaffolding and stays out of the plugin ZIP.
# Leading slashes are deliberate: an unanchored "vendor" would also drop
# assets/vendor/, which is where the Datastar runtime lives.
/.git
/.gitattributes
/.gitignore
/.github
/.claude
/.distignore
/bin
/tests
/e2e
/vendor
/composer.json
/composer.lock
/phpunit.xml.dist
/phpcs.xml.dist
/CONTRIBUTING.md
